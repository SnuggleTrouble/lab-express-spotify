<div>
  {{#each searchedArtists}}
    <div>
      <article>
        <h3>{{this.name}}</h3>
        <img src="{{images.0.url}}" alt="{{this.name}}" />
        <button>
          <a href="/albums{{this.id}}">View albums</a>
        </button>
      </article>
    </div>
  {{/each}}
</div>